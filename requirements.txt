# Windows: GPU-accelerated overlay (optional, falls back to Tkinter)
PySide6>=6.6.0; sys_platform == "win32"
openai>=1.0.0
openai-whisper
# Optional: faster local backend (CTranslate2)
faster-whisper
# Optional: fastest local backend on Apple Silicon (~4x faster)
# Note: Install with `pip install --no-deps lightning-whisper-mlx` to avoid tiktoken version conflict
lightning-whisper-mlx
deepgram-sdk>=3.0.0
groq>=0.4.0
sounddevice
soundfile
pyperclip
python-dotenv
rumps>=0.4.0
quickmachotkey>=2023.11.17
numpy
pynput
pyobjc-framework-Cocoa
pyobjc-framework-AVFoundation
typer>=0.9.0
